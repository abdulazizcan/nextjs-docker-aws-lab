{
  "name": "backend",
  "version": "1.0.0",
  "description": "This service provides the serverless backend for the project, handling user page data. It's built with the Serverless Framework and runs on AWS Lambda, API Gateway, and DynamoDB.",
  "main": "handler.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.893.0",
    "@aws-sdk/lib-dynamodb": "^3.893.0",
    "zod": "^4.1.11"
  }
}
